apiVersion: v1
kind: Service
metadata:
  name: debate-ai
  labels: { app: debate-ai }
spec:
  type: {{ .Values.service.type }}
  selector: { app: debate-ai }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080